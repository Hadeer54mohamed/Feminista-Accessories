import { createFeministaConfig } from "feminista-agent-playwright-config/config";

export default createFeministaConfig({
  // Add your custom playwright configuration overrides here
  // Example:
  // timeout: 60000,
  // use: {
  //   baseURL: 'http://localhost:3000',
  // },
});
